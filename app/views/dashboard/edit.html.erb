<%= turbo_frame_tag "main_content" do %>
  <%= render 'layouts/notice', notice: notice if notice %>
  <div class="max-w-2xl mx-auto">
    <div class="card">
      <header>
        <h2>Editing dashboard</h2>
        <p>Update the form below to modify this dashboard.</p>
      </header>
      <section>
        <%= render "form", dashboard: @dashboard %>
      </section>
    </div>

    <div class="flex flex-wrap gap-3 mt-6">
      <%= link_to "Show this dashboard", @dashboard, class: "btn-outline", data: { turbo_action: "advance" } %>
      <%= link_to "Back to dashboards", dashboards_path, class: "btn-outline", data: { turbo_action: "advance" } %>
    </div>
  </div>
<% end %>